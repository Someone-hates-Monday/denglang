# 本地 secret — 复制自本模板为 application-secret.yml（勿提交）
spring:
  datasource:
    password: "123456"

jwt:
  secret-key: the_secret_key_of_smart_street_light

mqtt:
  username: ""
  password: ""

# DeepSeek（OpenAI 兼容）；只需填写 api-key
llm:
  api-key: ""
  base-url: https://api.deepseek.com/v1
  model: deepseek-chat
  temperature: 0.3
  max-tokens: 2048
  top-k: 4
